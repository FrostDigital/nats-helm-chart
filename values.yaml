# Default values for nats.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
image:
  repository: quay.io/pires/docker-nats
  tag: 1.0.4
  pullPolicy: Always        
replicas: 3
resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 200m
    memory: 128Mi

